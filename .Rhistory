print(i)
## 5.2 Update equations algorithm
get_rls_params <- function(x_vals, y_vals, theta_0, R_0) {
# Number of data points
N <- length(x_vals)
# Lists to contain thetas and R-matrices
thetas <- vector("list", N)
Rs <- vector("list", N)
# Initializing theta_0 and R_0
theta <- theta_0
R <- R_0
for (i in 1: N) {
print(i)
# Retrieving previous values
theta_prev <- theta
R_prev <- R
# Retrieving data
x_val <- x_vals[[i]]
y_val <- y_vals[[i]]
# Constructing design matrix
X <- c(1, x_val)
# Computing next R matrix
R <- R_prev + X %*% t(X)
# Computing next parameter values
theta <- theta_prev + solve(R) %*% X * (y_val - t(X) %*% theta_prev)
# Appending new values to lists
thetas[[i]] <- theta
Rs[[i]] <- R
}
return (thetas)
}
get_rls_params(Dtrain$year, Dtrain$total, theta_0, R_0)
## 5.2 Update equations algorithm
get_rls_params <- function(x_vals, y_vals, theta_0, R_0) {
# Number of data points
N <- length(x_vals)
# Lists to contain thetas and R-matrices
thetas <- vector("list", N)
Rs <- vector("list", N)
# Initializing theta_0 and R_0
theta <- theta_0
R <- R_0
for (i in 1: N) {
print(i)
# Retrieving previous values
theta_prev <- theta
R_prev <- R
# Retrieving data
x_val <- x_vals[[i]]
y_val <- y_vals[[i]]
# Constructing design matrix
X <- c(1, x_val)
# Computing next R matrix
R <- R_prev + X %*% t(X)
# Computing next parameter values
print(theta_prev)
print(solve(R))
print(X)
print(y_val - t(X) %*% theta_prev)
theta <- theta_prev + solve(R) %*% X * (y_val - t(X) %*% theta_prev)
# Appending new values to lists
thetas[[i]] <- theta
Rs[[i]] <- R
}
return (thetas)
}
get_rls_params(Dtrain$year, Dtrain$total, theta_0, R_0)
print(R_0*10)
## 5.2 Update equations algorithm
get_rls_params <- function(x_vals, y_vals, theta_0, R_0) {
# Number of data points
N <- length(x_vals)
# Lists to contain thetas and R-matrices
thetas <- vector("list", N)
Rs <- vector("list", N)
# Initializing theta_0 and R_0
theta <- theta_0
R <- R_0
for (i in 1: N) {
print(i)
# Retrieving previous values
theta_prev <- theta
R_prev <- R
# Retrieving data
x_val <- x_vals[[i]]
y_val <- y_vals[[i]]
# Constructing design matrix
X <- c(1, x_val)
# Computing next R matrix
R <- R_prev + X %*% t(X)
# Computing next parameter values
print(theta_prev)
print(solve(R))
print(X)
print(y_val - t(X) %*% theta_prev)
print(solve(R) %*% X)
theta <- theta_prev + solve(R) %*% X * (y_val - t(X) %*% theta_prev)
# Appending new values to lists
thetas[[i]] <- theta
Rs[[i]] <- R
}
return (thetas)
}
get_rls_params(Dtrain$year, Dtrain$total, theta_0, R_0)
## 5.2 Update equations algorithm
get_rls_params <- function(x_vals, y_vals, theta_0, R_0) {
# Number of data points
N <- length(x_vals)
# Lists to contain thetas and R-matrices
thetas <- vector("list", N)
Rs <- vector("list", N)
# Initializing theta_0 and R_0
theta <- theta_0
R <- R_0
for (i in 1: N) {
print(i)
# Retrieving previous values
theta_prev <- theta
R_prev <- R
# Retrieving data
x_val <- x_vals[[i]]
y_val <- y_vals[[i]]
# Constructing design matrix
X <- c(1, x_val)
# Computing next R matrix
R <- R_prev + X %*% t(X)
# Computing next parameter values
print(theta_prev)
print(solve(R))
print(X)
print(y_val - t(X) %*% theta_prev)
print(solve(R) %*% X)
print(theta_prev + solve(R) %*% X * (y_val - t(X) %*% theta_prev))
theta <- theta_prev + solve(R) %*% X * (y_val - t(X) %*% theta_prev)
# Appending new values to lists
thetas[[i]] <- theta
Rs[[i]] <- R
}
return (thetas)
}
get_rls_params(Dtrain$year, Dtrain$total, theta_0, R_0)
print(theta_0 + matrix(c(1,1), nrow = 2, ncol = 1))
## 5.2 Update equations algorithm
get_rls_params <- function(x_vals, y_vals, theta_0, R_0) {
get_rls_params(Dtrain$year, Dtrain$total, theta_0, R_0)
## 5.2 Update equations algorithm
# Number of data points
N <- length(x_vals)
# Lists to contain thetas and R-matrices
thetas <- vector("list", N)
Rs <- vector("list", N)
# Initializing theta_0 and R_0
theta <- theta_0
R <- R_0
for (i in 1: N) {
print(i)
# Retrieving previous values
theta_prev <- theta
R_prev <- R
# Retrieving data
x_val <- x_vals[[i]]
y_val <- y_vals[[i]]
# Constructing design matrix
X <- c(1, x_val)
# Computing next R matrix
R <- R_prev + X %*% t(X)
# Computing next parameter values
print(solve(R) %*% X * (y_val - t(X) %*% theta_prev)
theta <- theta_prev + solve(R) %*% X * (y_val - t(X) %*% theta_prev)
## 5.2 Update equations algorithm
get_rls_params <- function(x_vals, y_vals, theta_0, R_0) {
# Number of data points
N <- length(x_vals)
# Lists to contain thetas and R-matrices
thetas <- vector("list", N)
Rs <- vector("list", N)
# Initializing theta_0 and R_0
theta <- theta_0
R <- R_0
for (i in 1: N) {
print(i)
# Retrieving previous values
theta_prev <- theta
R_prev <- R
# Retrieving data
x_val <- x_vals[[i]]
y_val <- y_vals[[i]]
# Constructing design matrix
X <- c(1, x_val)
# Computing next R matrix
R <- R_prev + X %*% t(X)
# Computing next parameter values
print(solve(R) %*% X * (y_val - t(X) %*% theta_prev))
theta <- theta_prev + solve(R) %*% X * (y_val - t(X) %*% theta_prev)
# Appending new values to lists
thetas[[i]] <- theta
Rs[[i]] <- R
}
return (thetas)
}
get_rls_params(Dtrain$year, Dtrain$total, theta_0, R_0)
## 5.2 Update equations algorithm
get_rls_params <- function(x_vals, y_vals, theta_0, R_0) {
# Number of data points
N <- length(x_vals)
# Lists to contain thetas and R-matrices
thetas <- vector("list", N)
Rs <- vector("list", N)
# Initializing theta_0 and R_0
theta <- theta_0
R <- R_0
for (i in 1: N) {
print(i)
# Retrieving previous values
theta_prev <- theta
R_prev <- R
# Retrieving data
x_val <- x_vals[[i]]
y_val <- y_vals[[i]]
# Constructing design matrix
X <- c(1, x_val)
# Computing next R matrix
R <- R_prev + X %*% t(X)
# Computing next parameter values
print(solve(R) %*% X)
print((y_val - t(X) %*% theta_prev))
theta <- theta_prev + solve(R) %*% X * (y_val - t(X) %*% theta_prev)
# Appending new values to lists
thetas[[i]] <- theta
Rs[[i]] <- R
}
return (thetas)
}
get_rls_params(Dtrain$year, Dtrain$total, theta_0, R_0)
## 5.2 Update equations algorithm
get_rls_params <- function(x_vals, y_vals, theta_0, R_0) {
# Number of data points
N <- length(x_vals)
# Lists to contain thetas and R-matrices
thetas <- vector("list", N)
Rs <- vector("list", N)
# Initializing theta_0 and R_0
theta <- theta_0
R <- R_0
for (i in 1: N) {
print(i)
# Retrieving previous values
theta_prev <- theta
R_prev <- R
# Retrieving data
x_val <- x_vals[[i]]
y_val <- y_vals[[i]]
# Constructing design matrix
X <- c(1, x_val)
# Computing next R matrix
R <- R_prev + X %*% t(X)
# Computing next parameter values
#print(solve(R) %*% X)
#print((y_val - t(X) %*% theta_prev))
print(solve(R) %*% X * (y_val - t(X) %*% theta_prev))
theta <- theta_prev + solve(R) %*% X * (y_val - t(X) %*% theta_prev)
# Appending new values to lists
thetas[[i]] <- theta
Rs[[i]] <- R
}
return (thetas)
}
get_rls_params(Dtrain$year, Dtrain$total, theta_0, R_0)
## 5.2 Update equations algorithm
get_rls_params <- function(x_vals, y_vals, theta_0, R_0) {
# Number of data points
N <- length(x_vals)
# Lists to contain thetas and R-matrices
thetas <- vector("list", N)
Rs <- vector("list", N)
# Initializing theta_0 and R_0
theta <- theta_0
R <- R_0
for (i in 1: N) {
print(i)
# Retrieving previous values
theta_prev <- theta
R_prev <- R
# Retrieving data
x_val <- x_vals[[i]]
y_val <- y_vals[[i]]
# Constructing design matrix
X <- c(1, x_val)
# Computing next R matrix
R <- R_prev + X %*% t(X)
# Computing next parameter values
#print(solve(R) %*% X)
#print((y_val - t(X) %*% theta_prev))
#print(solve(R) %*% X * (y_val - t(X) %*% theta_prev))
theta <- theta_prev + (solve(R) %*% X) * (y_val - t(X) %*% theta_prev)
# Appending new values to lists
thetas[[i]] <- theta
Rs[[i]] <- R
}
return (thetas)
}
get_rls_params(Dtrain$year, Dtrain$total, theta_0, R_0)
## 5.2 Update equations algorithm
get_rls_params <- function(x_vals, y_vals, theta_0, R_0) {
# Number of data points
N <- length(x_vals)
# Lists to contain thetas and R-matrices
thetas <- vector("list", N)
Rs <- vector("list", N)
# Initializing theta_0 and R_0
theta <- theta_0
R <- R_0
for (i in 1: N) {
print(i)
# Retrieving previous values
theta_prev <- theta
R_prev <- R
# Retrieving data
x_val <- x_vals[[i]]
y_val <- y_vals[[i]]
# Constructing design matrix
X <- matrix(c(1, x_val), ncol = 1)
# Computing next R matrix
R <- R_prev + X %*% t(X)
# Computing next parameter values
#print(solve(R) %*% X)
#print((y_val - t(X) %*% theta_prev))
#print(solve(R) %*% X * (y_val - t(X) %*% theta_prev))
theta <- theta_prev + (solve(R) %*% X) * (y_val - t(X) %*% theta_prev)
# Appending new values to lists
thetas[[i]] <- theta
Rs[[i]] <- R
}
return (thetas)
}
get_rls_params(Dtrain$year, Dtrain$total, theta_0, R_0)
## 5.2 Update equations algorithm
get_rls_params <- function(x_vals, y_vals, theta_0, R_0) {
# Number of data points
N <- length(x_vals)
# Lists to contain thetas and R-matrices
thetas <- vector("list", N)
Rs <- vector("list", N)
# Initializing theta_0 and R_0
theta <- theta_0
R <- R_0
for (i in 1: N) {
print(i)
# Retrieving previous values
theta_prev <- theta
R_prev <- R
# Retrieving data
x_val <- x_vals[[i]]
y_val <- y_vals[[i]]
# Constructing design matrix
X <- matrix(c(1, x_val), ncol = 1)
# Computing next R matrix
R <- R_prev + X %*% t(X)
# Computing next parameter values
#print(solve(R) %*% X)
#print((y_val - t(X) %*% theta_prev))
#print(solve(R) %*% X * (y_val - t(X) %*% theta_prev))
theta <- theta_prev + (solve(R) %*% X) * as.numeric(y_val - t(X) %*% theta_prev)
# Appending new values to lists
thetas[[i]] <- theta
Rs[[i]] <- R
}
return (thetas)
}
get_rls_params(Dtrain$year, Dtrain$total, theta_0, R_0)
thetas <- get_rls_params(Dtrain$year, Dtrain$total, theta_0, R_0)
print(thetas[[33]])
my_vect <- c(1,2,3,4,5)
my_vect[[:2]]
my_vect[:2]
my_vect[1:2]
my_vect[3:5]
thetas <- get_rls_params(Dtrain$year[1:3], Dtrain$total[1:3], theta_0, R_0)
print(thetas)
## 5.3 Final RLS params
thetas <- get_rls_params(Dtrain$year, Dtrain$total, theta_0, R_0)
print(last(thetas))
# 3. OLS - global linear trend model ----
## 3.1 Parameter estimation ----
# constructing the design matrix
X <- cbind(1, Dtrain$year)
print(X)
# constructing the y vector
y <- cbind(Dtrain$total)
print(y)
# estimating the parameters by solving the "normal equations":
theta_OLS <- solve(t(X)%*%X)%*%t(X)%*%y
print(theta_OLS)
# saving the parameter estimates as variables
theta_OLS_0 <- theta_OLS[1]
theta_OLS_1 <- theta_OLS[2]
## 3.2 Estimated standard errors and mean ----
# computing y_hat values (prediction)
yhat_OLS <- X%*%theta_OLS
Dtrain$yhat <- yhat_OLS
# Trying to modify the initial values such that the parameter estimates become more similar
theta_0 <- c(0,0.01)
thetas <- get_rls_params(Dtrain$year, Dtrain$total, theta_0, R_0)
print(last(thetas))
# Trying to modify the initial values such that the parameter estimates become more similar
theta_0 <- c(0,0.1)
thetas <- get_rls_params(Dtrain$year, Dtrain$total, theta_0, R_0)
print(last(thetas))
R_0 <- matrix(c(0.5, 0, 0, 0.5), nrow = 2, ncol = 2)
thetas <- get_rls_params(Dtrain$year, Dtrain$total, theta_0, R_0)
print(last(thetas))
R_0 <- matrix(c(1, 0, 0, 1), nrow = 2, ncol = 2)
thetas <- get_rls_params(Dtrain$year, Dtrain$total, theta_0, R_0)
print(last(thetas))
R_0 <- matrix(c(5, 0, 0, 5), nrow = 2, ncol = 2)
thetas <- get_rls_params(Dtrain$year, Dtrain$total, theta_0, R_0)
print(last(thetas))
# Trying to modify the initial values such that the parameter estimates become more similar
theta_0 <- c(0,0.1)
R_0 <- matrix(c(0, 0, 0, 0), nrow = 2, ncol = 2)
thetas <- get_rls_params(Dtrain$year, Dtrain$total, theta_0, R_0)
print(last(thetas))
R_0 <- matrix(c(1, 1, 1, 1), nrow = 2, ncol = 2)
thetas <- get_rls_params(Dtrain$year, Dtrain$total, theta_0, R_0)
print(last(thetas))
R_0 <- matrix(c(0.01, 0, 0, 0.01), nrow = 2, ncol = 2)
thetas <- get_rls_params(Dtrain$year, Dtrain$total, theta_0, R_0)
print(last(thetas))
R_0 <- matrix(c(0.001, 0, 0, 0.001), nrow = 2, ncol = 2)
thetas <- get_rls_params(Dtrain$year, Dtrain$total, theta_0, R_0)
print(last(thetas))
# Trying to modify the initial values such that the parameter estimates become more similar
theta_0 <- c(0,0)
R_0 <- matrix(c(0.001, 0, 0, 0.001), nrow = 2, ncol = 2)
thetas <- get_rls_params(Dtrain$year, Dtrain$total, theta_0, R_0)
print(last(thetas))
R_0 <- matrix(c(0.0001, 0, 0, 0.0001), nrow = 2, ncol = 2)
thetas <- get_rls_params(Dtrain$year, Dtrain$total, theta_0, R_0)
print(last(thetas))
R_0 <- matrix(c(0.00001, 0, 0, 0.00001), nrow = 2, ncol = 2)
thetas <- get_rls_params(Dtrain$year, Dtrain$total, theta_0, R_0)
print(last(thetas))
R_0 <- matrix(c(0.000001, 0, 0, 0.000001), nrow = 2, ncol = 2)
thetas <- get_rls_params(Dtrain$year, Dtrain$total, theta_0, R_0)
print(last(thetas))
# Trying to modify the initial values such that the parameter estimates become more similar
theta_0 <- c(0,1)
R_0 <- matrix(c(0.000001, 0, 0, 0.000001), nrow = 2, ncol = 2)
thetas <- get_rls_params(Dtrain$year, Dtrain$total, theta_0, R_0)
print(last(thetas))
# Trying to modify the initial values such that the parameter estimates become more similar
theta_0 <- c(1,1)
R_0 <- matrix(c(0.000001, 0, 0, 0.000001), nrow = 2, ncol = 2)
thetas <- get_rls_params(Dtrain$year, Dtrain$total, theta_0, R_0)
print(last(thetas))
theta_0 <- c(0,0)
R_0 <- matrix(c(0.1, 0, 0, 0.1), nrow = 2, ncol = 2)
lambda <- 0.7
thetas <- get_rls_params_w_forget(Dtrain$year, Dtrain$total, theta_0, R_0, lambda)
## 5.4 RLS with forgetting
get_rls_params_w_forget <- function(x_vals, y_vals, theta_0, R_0, lambda) {
# Number of data points
N <- length(x_vals)
# Lists to contain thetas and R-matrices
thetas <- vector("list", N)
Rs <- vector("list", N)
# Initializing theta_0 and R_0
theta <- theta_0
R <- R_0
for (i in 1: N) {
# Retrieving previous values
theta_prev <- theta
R_prev <- R
# Retrieving data
x_val <- x_vals[[i]]
y_val <- y_vals[[i]]
# Constructing design matrix
X <- matrix(c(1, x_val), ncol = 1)
# Computing next R matrix
R <- lambda * R_prev + X %*% t(X)
# Computing next parameter values
#print(solve(R) %*% X)
#print((y_val - t(X) %*% theta_prev))
#print(solve(R) %*% X * (y_val - t(X) %*% theta_prev))
theta <- theta_prev + (solve(R) %*% X) * as.numeric(y_val - t(X) %*% theta_prev)
# Appending new values to lists
thetas[[i]] <- theta
Rs[[i]] <- R
}
return (thetas)
}
thetas <- get_rls_params_w_forget(Dtrain$year, Dtrain$total, theta_0, R_0, lambda)
print(thetas)
mat <- do.call(rbind, thetas)
theta_0_vals <- mat[,1]
theta_1_vals <- mat[,2]
thetas <- get_rls_params_w_forget(Dtrain$year, Dtrain$total, theta_0, R_0, lambda)
print(thetas)
mat <- do.call(rbind, thetas)
theta_0_vals <- mat[,1]
theta_1_vals <- mat[,2]
theta_0_vals
mat
thetas <- get_rls_params_w_forget(Dtrain$year, Dtrain$total, theta_0, R_0, lambda)
print(thetas)
mat <- do.call(cbind, thetas)
theta_0_vals <- mat[,1]
theta_1_vals <- mat[,2]
theta_0_vals
mat
print(thetas[[3]])
print(thetas[[3]][1])
